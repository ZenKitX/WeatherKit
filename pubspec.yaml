name: weather_kit
description: A weather service package for Flutter apps. Provides weather data fetching, parsing, and caching.
version: 0.1.0
homepage: https://github.com/ZenKitX/WeatherKit
repository: https://github.com/ZenKitX/WeatherKit
issue_tracker: https://github.com/ZenKitX/WeatherKit/issues

environment:
  sdk: ^3.11.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
